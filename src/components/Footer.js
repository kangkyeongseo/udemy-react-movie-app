import React from "react";
import Styles from "styles/Footer.module.css";

const Footer = () => {
  return (
    <footer className={Styles.footer}>
      <span>&copy; Movie App</span>
    </footer>
  );
};

export default Footer;
